{
  "name": "trigo-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=https://salvadorcardona.github.io/trigo-js/",
    "deploy-local": "gh-pages -d dist",
    "deploy-gh": "pnpm build && pnpm deploy-local",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}